// Recessive surface shared by info chips, score group titles, score bar
// (inactive segments) and callout captions. Darker in light mode for contrast;
// dark mode keeps the muted/50 fill which already reads well.
export const SURFACE_CLASS = "bg-chip-surface dark:bg-muted/50";
